# deliverables/

Client-ready exports — promoted from `drafts/` after approval.

## Currently empty

The only "deliverable" so far is the DuckDB mirror at `.local_tier/clients/open-seo.duckdb`, which is consumed by the live OpenSEO MCP and any analytical queries Dennis runs. It doesn't need to live here — it's regenerated by `scripts/sync-openseo-duckdb.py` on demand.