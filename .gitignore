# Sensitive — Icarus session fabric
fabric/

# SQLite lock files
*.db-wal
*.db-shm

# OS
.DS_Store
Thumbs.db

# graphify regenerable cache (per-file AST) — rebuild locally
graphify-out/cache/
graphify-out/converted/
# graphify dated dump dirs (regenerated daily, 800k+ per day, not source of truth)
graphify-out/2026-*/

# Nested git clones (separate repos, not wiki content)
/agency-agents/
/raw/repos/
/tech-debt/github/

# Per-client analytical mirrors (DuckDB) — regenerated by cron, not source of truth
.local_tier/
*.duckdb
*.duckdb.wal
