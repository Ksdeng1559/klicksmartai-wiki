# Sensitive — Icarus session fabric
fabric/

# SQLite lock files
*.db-wal
*.db-shm

# OS
.DS_Store
Thumbs.db

# graphify regenerable cache (per-file AST) — rebuild locally
graphify-out/
# Keep graphify-out/ structure on disk; gitignore excludes all regenerable artifacts

# Nested git clones (separate repos, not wiki content)
/agency-agents/
/raw/repos/
/tech-debt/github/

# Per-client analytical mirrors (DuckDB) — regenerated by sync, not source of truth.
# README.md + scripts/ stay tracked so the mirror tooling is reviewable in git.
.local_tier/*.duckdb
.local_tier/*.duckdb.wal
.local_tier/clients/
.local_tier/logs/
*.duckdb
*.duckdb.wal
