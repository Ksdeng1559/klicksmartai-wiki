# skills/

Client-specific Hermes skills for OpenSEO. Skills live canonically at `/home/denni/wiki/hermes/skills/` and are referenced from the wiki's `external_dirs`. This folder holds a per-client **registry** for tracking what's bound to this engagement.

## Currently empty

No client-specific skills yet. Generic skills used for this client (loaded from the wiki canonical):
- `icm-client-workspace-setup` (used to scaffold this workspace)
- `hermes-mcp-cmd-windows` (for adding npx-based MCP servers on WSL)
- `github-fork-fallback` (for push-denied scenarios)

If you build a client-specific skill (e.g. `openseo-deploy` for the rebuild + restart sequence), register it here with a one-line description and the path.