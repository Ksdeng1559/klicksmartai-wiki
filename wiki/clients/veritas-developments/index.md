---
okf_version: "0.2"
manifest:
  client: veritas-developments
  version: 2026-08-29
  generator: icm-client-workspace-setup v1.0.0
---

# Veritas Development Group LLC — Bundle Index

> Progressive disclosure: read this file → `IDENTITY.md` → `CONTEXT.md`. That's enough orientation to act. Any LLM that reads markdown + YAML frontmatter (ChatGPT, Claude, Gemini, Hermes, Mistral, anything) can ingest this bundle without installing any skill.

## Identity

* [Identity](IDENTITY.md) — who is the client, principals, engagement overview (type: `Client Workspace`)
* [Context](CONTEXT.md) — what leaves this workspace, current pipeline stage (type: `Client Engagement`)

## Configuration (factory)

* [Voice](_config/voice.md) — type: `Reference`
* [Conventions](_config/conventions.md) — type: `Reference`
* [Deliverables](_config/deliverables.md) — type: `Reference` (vertical artifact map)
* [GTM Skills](_config/gtm-skills.md) — type: `SkillBinding` (per-client skill roster + HITL gate + Deepline CLI rule)
* [Glossary](_config/glossary.md) — type: `Glossary`
* [Compliance](_config/compliance.md) — type: `Compliance` (Reg D 506(b) overlay)

## Drafts (pre-validation, AI WIP)

Every `.md` here carries `status: draft`. Source-of-truth gate: nothing promotes to `projects/` until Dennis + the relevant client reviewer approve via `VALIDATION_QUEUE.md`.

* [Drafts root](drafts/) — every `.md` is `status: draft`
* [Validation queue](drafts/VALIDATION_QUEUE.md)

## Projects (validated, source of truth)

Every `.md` here carries `status: stable` and is `human-reviewed` per OKF §5.3.

* [Projects root](projects/)

## Deliverables (client-ready exports)

* [Deliverables root](deliverables/)

## SOPs (Special SOPs and runbooks)

Company knowledge — stable SOPs the team refers to. Each is `type: Playbook` with parameters + executor + attester where callable.

* [CDFI 7-touch outreach](sop/cdfi-7-touch-outreach.md) — type: `Playbook` (callable, see frontmatter)

**Conformance status (2026-08-29):** this bundle has **85 `.md` files, all OKF-conformant** (parseable YAML frontmatter carrying a non-empty `type:`). One documented exception: `CLAUDE.md` is regenerated by `graphify` on sync and does not carry OKF frontmatter — it serves as the Hermes/Claude folder-entry adapter, not as an OKF concept node. Consumers that want full OKF coverage can skip `CLAUDE.md` and read `IDENTITY.md` + `CONTEXT.md` instead.

## Knowledge graph ingestion (for any LLM)

1. Read this `index.md` — the bundle manifest, declares `okf_version: "0.2"`
2. Read [`IDENTITY.md`](IDENTITY.md) — the who/why orientation
3. Walk the graph by following bundle-relative links (`/path/file.md`) or relative links in any concept body
4. Read frontmatter on any concept to know its `type`, `status`, `verified` (trust tier), `sources[]`, and call signature if `parameters[]` is present

If you can read YAML frontmatter, you can read this bundle. No skill install required. No API gateway. That is the OKF promise and the reason every concept here carries frontmatter.

## Special SOP — first proof

The CDFI 7-touch outreach SOP at `sop/cdfi-7-touch-outreach.md` is the first company-knowledge concept authored to the OKF callable contract. It carries `type: Playbook` + `parameters[]` + `executor` + `attester` — meaning any LLM can read it as a function and invoke it. David Poole (Veritas Founder) is named in `verified[]` as the human reviewer per Reg D 506(b) gating.
