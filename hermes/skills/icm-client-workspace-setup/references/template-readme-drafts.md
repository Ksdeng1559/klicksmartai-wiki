# drafts/ — AI Work in Progress (pre-HITL)

**Purpose:** Working hypotheses built from **public web research + AI inference** — NOT source-of-truth. Everything here requires human validation before it can be promoted.

**The rule (source-of-truth gate):** Files in `drafts/` are **not** approved deliverables. They are hypotheses that require Dennis's confirmation before being promoted to `projects/` or `deliverables/`. See `VALIDATION_QUEUE.md` for the exact gating.

## Current drafts
_Empty by default. Create `VALIDATION_QUEUE.md` here the first time a draft is added._

## Procedure
Read `VALIDATION_QUEUE.md` first. Only promote a draft when its approvers have confirmed. Never promote without an explicit go.
