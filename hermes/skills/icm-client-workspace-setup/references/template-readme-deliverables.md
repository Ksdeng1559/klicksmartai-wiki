# Deliverables — <client_name>

This file is the **canonical artifact-type map** for <client_name>. It tells the agent (and the user) where to put each kind of deliverable, and which Herm...[truncated]