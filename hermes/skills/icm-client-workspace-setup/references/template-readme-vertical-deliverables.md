# deliverables/<vertical>/ — Client-Ready <Vertical> Exports

**Purpose:** Client-ready <vertical> deliverables for <client_name>.

**Rule:** A deliverable only lands here after HITL approval.

## Files

_Empty by default. Populate from `projects/<vertical>/` after promotion._

## Export procedure

1. Promote approved `.md` source to `projects/<vertical>/`.
2. Copy / render the final asset here (`<name>-<YYYY-MM-DD>.<ext>`).
3. Reference the source in `projects/<vertical>/`.
4. Update the Skills Registry in `skills/README.md` if this is a new artifact class.
